# Files

This directory contains some helpers for ingesting and reading from the blog
feed.
This code is used on the server to build an RSS representation of the articles
in the `entries` folder.
`mod.ts` contains exports used by server code, so start there when reading
for the first time.
